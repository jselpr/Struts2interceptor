<!DOCTYPE validators PUBLIC "-//Apache Struts//XWork Validator 1.0.3//EN"
        "http://struts.apache.org/dtds/xwork-validator-1.0.3.dtd">
<validators>
    <field name="usuario">
        <field-validator type="required">
            <message>${getText("obligatorio")}</message>
        </field-validator>
    </field>
    <field name="clave">
        <field-validator type="required">
            <message>${getText("obligatorio")}</message>
        </field-validator>
    </field>
</validators>